.Side-bar
  .Side-header
    .Side-header__box
      .Side-header__box-name
        = current_user.name
      .Side-header__box-icon
        = link_to new_group_path, class: "Side-header__box-icon--new" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "Side-header__box-icon--edit" do
          = icon('fas', 'cog')
  .Group
    - current_user.groups.each do |group|
      .Group__box
        = link_to '#', class: "Group__box-content" do
          .Group__box-name
            = group.name
          .Group__box-message
            メッセージはまだありません